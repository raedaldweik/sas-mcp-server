name: Publish container image to GHCR

on:
  push:
    branches:
      - main
      # Feature branches that need a pullable image before they merge — e.g. a
      # deployment being wired up in SAS Retrieval Agent Manager, which can only
      # consume a published image, never a local build.
      - 'claude/**'
    tags:
      - 'v*'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # `value=` is a type=raw attribute; type=ref ignores it and emits the
            # branch name, so this published `main` and never the documented
            # `edge` that deploy/docker.md tells people to pull.
            type=raw,value=edge,enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
            # Branch builds get a tag named after the branch (slashes become
            # dashes), so a work-in-progress deployment has a stable ref to pull.
            type=ref,event=branch,enable=${{ startsWith(github.ref, 'refs/heads/claude/') }}
            type=sha,format=short,prefix=sha-
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build and push image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          # arm64 is emulated under QEMU and roughly triples the build; releases
          # and `edge` are worth it, an iterating feature branch is not.
          platforms: ${{ startsWith(github.ref, 'refs/heads/claude/') && 'linux/amd64' || 'linux/amd64,linux/arm64' }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

  # Lists the image just pushed on the public MCP Registry
  # (registry.modelcontextprotocol.io), which is what feeds the GitHub MCP
  # Registry and other client-side catalogues. Metadata only — the artifact
  # stays on GHCR.
  publish-mcp-registry:
    # MUST run after the image exists. The registry validates ownership by
    # fetching ghcr.io/<repo>:<version> anonymously and reading the
    # io.modelcontextprotocol.server.name label off its config, so a standalone
    # workflow on the same tag would race the build and fail with
    # "OCI image ... does not exist in the registry".
    needs: build-and-push
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # OIDC is what makes the io.github.sassoftware namespace reachable at all:
      # the registry reads the `repository_owner` claim off the Actions token and
      # grants io.github.<owner>/*. Publishing by hand instead would require the
      # human running it to be an Owner of the sassoftware org.
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install mcp-publisher
        run: |
          curl -fsSL "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_linux_amd64.tar.gz" \
            | tar xz mcp-publisher

      - name: Pin server.json to the released version
        # Derived from the tag rather than trusted from the file, so a forgotten
        # bump cannot publish metadata pointing at the previous image.
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          jq --arg v "$VERSION" --arg img "${REGISTRY}/${IMAGE_NAME}:${VERSION}" \
            '.version = $v | .packages[0].identifier = $img' server.json > server.tmp
          mv server.tmp server.json
          cat server.json

      - name: Authenticate to the MCP Registry
        run: ./mcp-publisher login github-oidc

      - name: Publish
        run: ./mcp-publisher publish
